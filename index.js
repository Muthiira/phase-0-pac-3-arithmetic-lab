function add(x, y) {
	console.log(add);
}

function subtract(x, y) {
	console.log(subtract);
}

function divide(x, y) {
	console.log(divide);
}

function multiply(x, y) {
	console.log(multiply);
}

function add(x, y) {
	return (x + y);
}

function subtract(x, y) {
	return (x - y);
}
function divide(x, y) {
	return (x / y);
}

function multiply(x, y) {
	return (x * y);
}
function increment(x) {
	return (x + 1)
}
function decrement(x) {
	return (x - 1)
}

function makeInt(n, base10) {
	let a = parseInt(n, base10);
	return a;
}

function preserveDecimal(n,base10){
	let a = parseFloat(n,base10);
	 return a;
}
